export interface UpdateUserArgs {
  skills: string[]; // array of ids
  studyProgram: string[]; // array of ids
  mobile: string;
  preferences: string[]; // array of ids
  bio: string;
  languages: string[]; // array of ids
  courses: string[]; // array of ids
}
